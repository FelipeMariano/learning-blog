<h1> Nova postagem </h1>

<%= form_for :post, url: posts_path do |f| %>
	<p>
		<%= f.label 'Título' %>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label 'Texto' %>
		<%= f.text_area :body %>
	</p>
	<%= f.submit 'Salvar' %>
<% end %>